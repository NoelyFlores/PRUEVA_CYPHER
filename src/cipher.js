window.cipher = {
  
 

  
};
// function cadena(string){

// }
function encode(offset, cadena){
	for (var i = 0; i <= cadena.length; i++) {
		var letra=[];
		//letra[i];
		//return letra[i];
		console.log(letra[i]+" ")
	}
}